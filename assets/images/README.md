This folder is referenced in `pubspec.yaml` under `flutter: assets:`.
Add your app images here (e.g. onboarding illustrations, icons used
inside the UI). This placeholder file exists only so the empty folder
is tracked by git — delete it once you add real images.
